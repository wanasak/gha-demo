name: Or Condition
on: workflow_dispatch
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Step one
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
        run: echo "This step will only run if the branch is main or develop"
      - name: Step two
        run: echo "This step will always run"